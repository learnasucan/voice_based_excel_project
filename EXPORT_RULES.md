# Export Rules

Exports use separate columns:

Serial, Name (MR), Name (EN), Entry Type, Amount, Gift (MR), Gift (EN), Place (MR), Place (EN)

Amount stays numeric for cash rows. Gift-only rows export a blank amount. Total rows sum only `cash` and `cash_and_gift` entries.
