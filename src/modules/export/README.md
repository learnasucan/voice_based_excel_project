# Export Module (Reserved)

This folder is intentionally scaffold-only in Worker A.

## Allowed public exports

- Re-export DTOs and `ExportService` interface from `src/contracts`.

## Reserved for export worker implementation

- format builders (txt/json/pdf/docx)
- storage adapter
- export job orchestration
- status polling implementation
